import React from "react";

const Notice = () => {
  return (
    <div className="notice">
      <p>
        ⚠️ <strong>MVP Admin Access Only:</strong> This prototype runs on <strong>localhost</strong> without a backend.
        Future versions will integrate smart contracts for real-time insurance payouts.
      </p>
    </div>
  );
};

export default Notice;
